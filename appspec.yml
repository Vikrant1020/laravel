version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/html/
hooks:
  AfterInstall:
    - location: laravel.sh
      timeout: 600
  #ApplicationStart:
   # - location: start.sh
    #  timeout: 100
